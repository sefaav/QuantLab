# Common runtime requirements for QuantLab.
# For full development, prefer: pip install -e ".[dev,dashboard,yahoo,extra,docs,notebooks]"
# This file includes the core dependencies plus the Yahoo and dashboard extras.

numpy>=2.5.2
pandas>=2.1
pyarrow>=14.0
scipy>=1.11
statsmodels>=0.14
matplotlib>=3.8
plotly>=5.18
pydantic>=2.5
pyyaml>=6.0
typer>=0.27.1
requests>=2.31
pandas-market-calendars>=5.4,<6
filelock>=3.32.3

# Data source (optional but needed for `quantlab download` from Yahoo)
yfinance>=0.2.40

# Dashboard (optional)
# The dashboard uses Streamlit 1.61's lazy-tab API.
streamlit>=1.61.1
